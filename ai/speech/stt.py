"""Speech-to-text module (placeholder).

Replace the body of `transcribe` with a real STT implementation
(e.g. OpenAI Whisper, Google Speech-to-Text) when ready.
"""


def transcribe(audio_bytes: bytes) -> str:
    """Convert raw audio bytes to a text transcript.

    Currently returns a placeholder string.  Integrate a real STT
    engine (Whisper, GCP Speech-to-Text, etc.) here.
    """
    # TODO: integrate a real speech-to-text engine
    return "placeholder transcript"
