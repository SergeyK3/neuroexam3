# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9    # required for Form / File uploads in FastAPI

# Settings / .env support
pydantic-settings>=2.2.0

# Async HTTP client (used by speech & evaluation services)
httpx>=0.27.0

# Telegram bot (uncomment and pin when implementing the bot)
# python-telegram-bot>=21.0

# Whisper (STT) via OpenAI API — нужен OPENAI_API_KEY в .env
openai>=1.40.0

# Google Sheets (эталоны с листа ideal_answers)
gspread>=6.0.0
google-auth>=2.29.0
# SpeechRecognition>=3.10.0  # local / Google STT

# Очередь задач (Redis + arq) — опционально, см. REDIS_URL в .env
arq>=0.26.0
# Асинхронный клиент Redis для SessionStore (сессии экзамена переживают рестарт)
redis>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # also used as async test client
